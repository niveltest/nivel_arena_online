# Task: Fine-tuning Playmat Zone Alignment

## Status

- [ ] Add `gap` configuration to Playmat Config
  - [ ] Update `PlaymatZoneConfig` interface or add explicit `gap` property to `field`.
  - [ ] Update CSS variable injection in `<style>` block.
  - [ ] Update `renderSlots` to use the dynamic gap.
- [ ] Adjust `official` Theme Coordinates
  - [ ] Field Top: Adjust to `26%` (Trial).
  - [ ] Field Gap: Set to `2rem` (Trial).
- [ ] Verify Alignment

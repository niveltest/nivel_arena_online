# カードステータス表示調整計画 (最終版)

アイコンがカード枠で切れないようレイアウトを調整し、パワーとヒット数の配置を最適化します。

## 変更内容

### 1. カードステータスおよびコンテナの表示修正 (`Card.tsx`)

* [MODIFY] [Card.tsx](file:///c:/Users/worke\Antigravity\nivel_arena_online\components\Card.tsx)
  * **コンテナの見切れ解消**: 親要素の `overflow-hidden` を解除し、代わりに中の画像エリア等に必要な部分だけにクリッピングを適用します。これにより、枠外にはみ出しているコスト・パワー・ヒット数のアイコンが綺麗に見えるようになります。
  * **パワー (Power)**: 位置を右上に配置します。
  * **ヒット数 (Hit Count)**: パワー表示の**すぐ下**（カード右側）に配置します。

## 検証プラン

* ビルドが通ることを確認。
* （ユーザー検証）
  * コスト、パワー、ヒット数のアイコンが欠けることなく、綺麗な形で枠外にはみ出して見えていること。
  * ヒット数がパワーの直下に配置されていること。

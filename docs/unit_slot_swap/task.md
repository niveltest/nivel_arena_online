# タスク：相手側ユニットスロットの反転（対向配置の正常化）

## ステータス

- [x] 実装計画の作成
- [x] `GameBoard.tsx` の `PlaymatArea` 内のマップ処理の修正
  - [x] 相手側（`isOpponent: true`）の場合のみ、インデックス `0` と `2` を入れ替えてレンダリング
- [x] 対向ユニットが正しく正対（正面に並ぶ）しているか確認
- [x] 動作確認

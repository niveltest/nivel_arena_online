# 実装計画 - ユニットゾーンの水平間隔の微調整

スロット 0 と 2 を、中央のスロット 1 側に 5% ずつ近づけ、より中央に寄せた配置にします。

## 変更内容

### [GameBoard.tsx](file:///c:/Users/worke/Antigravity/nivel_arena_online/components/GameBoard.tsx)

#### 1. 水平座標の再計算

現行の間隔 (18%) から 5% 縮小し、13% の間隔にします。

- **field1 (中央)**: `50.0%` (据え置き)
- **field0 (左)**: `32.0%` → `37.0%` (5% 右へ移動)
- **field2 (右)**: `68.0%` → `63.0%` (5% 左へ移動)

垂直方向の配置 (`top: 20.0%`) とスケール (`0.8`) は維持します。

## 検証計画

### 手動確認

- 編集モードでリセットし、スロット 0 と 2 がこれまでより内側に配置されていることを確認。
- 相手側の対向ユニットも同様に内側に配置され、垂直・水平の両方で正対関係が維持されているか確認。
- プレイマット背景のユニット枠（内側寄りにある場合）との整合性が向上しているか確認。

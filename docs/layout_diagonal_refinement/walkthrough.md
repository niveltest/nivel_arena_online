# 修正内容の確認 - 対角線配置の最適化

相手の配置を完全に「対角線（点対称）」にしつつ、カードやテキストが逆さまにならずプレイヤーに向くように調整しました。

## 変更内容

### [GameBoard.tsx](file:///c:/Users/worke/Antigravity/nivel_arena_online/components/GameBoard.tsx)

#### 1. 完璧な点対称配置

- プレイマットコンテナ全体を `rotate(180deg)` することで、自分の座標（例：左下）に対して相手の座標（例：右上）が自動的に対角線上に配置されるようにしました。

#### 2. カード・UIの向きの補正

- 相手側の `DraggableZone` 内の要素をさらに `rotate(180deg)` させることで、配置は対角線上にありつつも、カードの絵柄やテキストはプレイヤーから見て正しい向き（正対）になるように修正しました。
- レベルサイドバーの数字（1〜10）についても同様に、相手側では回転させて読みやすくしました。

## 検証結果

- [x] 自分のリーダー（左下付近）に対し、相手のリーダーが右上付近に正しく配置されている。
- [x] 相手のカードが自分の方を向いており、テキストが読める状態である。
- [x] 編集ツールで自分の配置を動かすと、相手側の点対称な位置も連動して適切に扱える（サーバー同期時）。

これにより、対戦中の視認性と「対面感」が大幅に向上しました。

# 無属性カードの削除と属性割り当て

## 調査

- [ ] **量産型ニケの属性調査**
  - [ ] Soldier EG, OW, FA (エリシオン) の属性（コード）
  - [ ] Product 08, 12, 23 (ミシリス) の属性
  - [ ] IDoll Flower, Ocean, Sun (テトラ) の属性
  - [ ] ニケのコード（火、水、風、鉄甲、電撃）とニベルアリーナの属性（炎、波濤、嵐、大地、稲妻）の対応関係の確定。

## 実装

- [ ] **`cards.json` の更新**
  - [ ] 量産型ニケに正しい `attribute` を設定する。
  - [ ] もしカード数が不足する場合、既存カードの属性バリエーションを増やすか、各属性に量産型を振り分ける。
- [ ] **`Game.ts` の修正**
  - [ ] `getCompatibleCards` で `!c.attribute` (無属性許可) を削除し、完全一致のみにする。

## 検証

- [ ] **デバッグスクリプト実行**
  - [ ] `debugDeckGen.ts` を実行し、無属性カードが混入しないこと、かつ有効なデッキが組める（枚数が足りる）ことを確認する。

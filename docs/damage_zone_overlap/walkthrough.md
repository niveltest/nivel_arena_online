# 修正内容の確認 - ダメージゾーンのカード重ね合わせ表示

ダメージゾーンの表示を、単なるマス目から提供されたイメージ画像に近い「カードの積み重ね」形式に変更しました。

## 変更内容

### [GameBoard.tsx](file:///c:/Users/worke/Antigravity/nivel_arena_online/components/GameBoard.tsx)

#### 1. カードスタック形式のダメージ表示

- `damageZone` にあるカードを `Card` コンポーネントとしてレンダリングするように変更しました。
- 負のマージン（`-1.6rem`）を使用して、カードが左から順に重なって見えるように調整しました。
- 各カードは小型（`scale: 0.4`）に設定し、10枚程度重なってもプレイマットに収まるようにしています。

#### 2. ガイドスロットの追加

- 背景に 10 個の斜めのスリット（ガイド）を表示し、プレイマット画像のデザインと馴染むようにしました。

#### 3. 数値表示の強化

- 現在のダメージ数（Hits）を強調するバッジを追加し、視認性を高めました。

#### 4. 対角線配置の継承

- `DraggableZone` の特性を活かし、相手側のダメージゾーンでもカードが正対（プレイヤーから見て正しい向き）して表示されるように設定しました。

## 検証結果

- [x] ダメージを受けると、カードが実際に表示され、重なり合って配置されることを確認。
- [x] リサイズツールを使用して、ダメージゾーン全体のサイズをプレイマットのスロットに合わせられることを確認。
- [x] 相手側のカード向きが自分と同じ（正対）になっていることを確認。

今後の座標調整は、レイアウトエディタを使用して新しいスロット位置に合わせてください。

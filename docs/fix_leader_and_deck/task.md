# リーダー能力の修正とデッキ構築ロジックの修正

## リーダー能力（テキスト）の修正

- [x] **リーダー情報の調査**
  - [x] レッドフード、ドロシー、紅蓮、クラウン、プリバティの覚醒前/覚醒後のテキストを調査する。
- [x] **`server/data/cards.json` の更新**
  - [x] 各リーダーの `text` (通常時) と `awakenedText` (覚醒時) を正しい内容に更新する。
  - [x] **新規実装**: `effects` に `ON_AWAKEN` (覚醒時効果) を追加。
- [x] **`Game.ts` のロジック実装**
  - [x] ターン開始時にレベル3到達で覚醒イベントを発火。
  - [x] `ON_AWAKEN` トリガーの実装。
  - [x] 新しいアクション (`STUN_UNIT`, `DEBUFF_ENEMY`, `HEAL_LEADER`) のサポート。

## デッキ構築ロジックの修正

- [x] **原因調査**
  - [x] `server/Game.ts` の `getCompatibleCards` ロジックを確認（スクリプト検証済み：正常）。
  - [x] `cards.json` の各カードの `attribute` を確認（すべて正常）。
- [x] **修正実装**
  - [x] 覚醒ロジックの導入により、リーダーの状態遷移が明確化。
  - [x] 共通型定義 (`shared/types.ts`) の更新で整合性を確保。

## 検証

- [x] **検証用スクリプト** (`server/testData/unitTestAwakening.ts`)
  - [x] ターン経過によるレベルアップと覚醒の動作確認。
  - [x] 覚醒効果（バフ、アイテム回収など）の適用確認。
